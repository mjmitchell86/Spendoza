[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    auth: tests requiring authentication
    public: tests for public (unauthenticated) pages
    mobile: tests for mobile viewport
    smoke: quick smoke tests
addopts = -v --tb=short
