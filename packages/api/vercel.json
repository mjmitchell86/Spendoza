{
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "installCommand": "cd ../.. && bun install"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api" }
  ]
}
